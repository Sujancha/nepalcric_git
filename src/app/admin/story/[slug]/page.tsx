// Admin mirror for /story/[slug] — inherits AdminBar via admin layout
import StoryArticlePage from '@/app/story/[slug]/page';
export { generateMetadata } from '@/app/story/[slug]/page';
export default StoryArticlePage;
